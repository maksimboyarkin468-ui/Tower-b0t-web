# Tower Bot AI - Веб-приложение

Веб-версия Telegram бота Tower Bot AI для игры Tower Rush.

## 🔐 Система доступа

Веб-приложение теперь работает **только для пользователей, которые**:
- ✅ Подписаны на канал @maksoncikaz
- ✅ Внесли депозит через реферальную ссылку

## 🚀 Как запустить

### 1. Запустите Telegram бота
Бот должен быть запущен, так как веб-приложение обращается к его API:
```bash
cd C:\Users\boiar\tower-bot-telegram
py bot.py
```

### 2. Откройте веб-приложение через Telegram бота
Веб-приложение должно открываться через кнопку "🌐 Открыть Web-App" в Telegram боте.

**Важно:** Веб-приложение работает только при запуске через Telegram, так как использует Telegram Web App API для получения данных пользователя.

## 📋 Как это работает

1. **Пользователь открывает веб-приложение через Telegram бота**
   - Telegram передает данные пользователя (user_id) через Web App API

2. **Автоматическая проверка доступа**
   - Веб-приложение отправляет запрос к API бота (`http://localhost:8080/api/check_user`)
   - Бот проверяет в базе данных:
     - Подписан ли пользователь на канал
     - Внес ли пользователь депозит

3. **Результат проверки**
   - ✅ Если все проверки пройдены → открывается главное меню
   - ❌ Если проверки не пройдены → показывается сообщение с инструкциями

## 🔧 Технические детали

### API Endpoint
- **URL:** `http://localhost:8080/api/check_user`
- **Method:** POST
- **Body:** `{ "user_id": 123456789 }`
- **Response:** 
  ```json
  {
    "has_access": true/false,
    "has_deposit": true/false,
    "is_subscribed": true/false,
    "message": "Доступ разрешен"
  }
  ```

### Зависимости
- Telegram Web App SDK (загружается автоматически)
- HTTP API бота (запускается вместе с ботом на порту 8080)

## 🧪 Тестирование

Для тестирования можно добавить `?user_id=ВАШ_ID` в URL:
```
https://imaginative-selkie-a5d3fc.netlify.app/?user_id=1226518807
```

## 📁 Структура файлов

```
tower-bot-web/
├── index.html      (главная страница)
├── style.css       (стили оформления)
├── script.js       (логика работы и проверка доступа)
├── README.md       (эта инструкция)
└── images/         (папка с картинками)
    ├── tower-rush.jpg
    ├── tsdss4.jpg
    └── ...
```

## ⚠️ Важные замечания

1. **Бот должен быть запущен** - веб-приложение не работает без запущенного бота
2. **Только через Telegram** - приложение использует Telegram Web App API
3. **Проверка в реальном времени** - статус проверяется каждый раз при открытии приложения
4. **CORS настроен** - API бота разрешает запросы с любого источника

## 🐛 Решение проблем

### Ошибка "Ошибка подключения к серверу"
- Убедитесь, что бот запущен
- Проверьте, что порт 8080 свободен
- Проверьте логи бота на наличие ошибок

### Ошибка "Приложение должно быть запущено через Telegram бота"
- Откройте веб-приложение через кнопку в Telegram боте
- Не открывайте напрямую в браузере (кроме тестирования с параметром user_id)

### Доступ запрещен
- Убедитесь, что вы подписаны на канал @maksoncikaz
- Убедитесь, что ваш депозит подтвержден администратором
- Свяжитесь с менеджером @nomep999
